#' @param message_log (`character(1)`)\cr
#' Path to the message log files e.g. `/tmp/message_logs/`
#' The message log files are named `message_<worker_id>.log`.
#' If `NULL`, no messages, warnings or errors are stored.

